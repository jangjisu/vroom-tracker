# 운영 컨테이너 환경변수 템플릿.
# 실제 값을 채운 뒤 `.env` 로 복사하세요. (.env 는 git 에 올라가지 않습니다)
#   cp .env.example .env

# 한국도로공사 공공데이터 키 (data.ex.co.kr)
EX_API_KEY=

# 카카오 REST API 키 — 서버 전용 시크릿
KAKAO_REST_API_KEY=

# 네이버 지도 JS key id — 브라우저에 노출되므로 콘솔에서 도메인 제한 필수
NAVER_MAPS_NCP_KEY_ID=
